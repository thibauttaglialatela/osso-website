{% extends '../../base.html.twig' %}

{% block title %}MusicalWork{% endblock %}

{% block body %}
    <h1 class="text-center">{{ musical_work.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ musical_work.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ musical_work.title }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ musical_work.status }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-secondary" href="{{ path('app_musical_work_crud_index') }}">Revenir à la liste</a>

    <a class="btn btn-outline-info" href="{{ path('app_musical_work_crud_edit', {'id': musical_work.id}) }}">Editer</a>

    {{ include('musical_work_crud/_delete_form.html.twig') }}
{% endblock %}
