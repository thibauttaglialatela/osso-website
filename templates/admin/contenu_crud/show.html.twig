{% extends 'admin/base.html.twig' %}

{% block title %}{{ parent() }} - Contenu{% endblock %}

{% block body %}
    <h1 class="text-center">Contenu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Image</th>
                <td>{{ contenu.poster }}</td>
            </tr>
            <tr>
                <th>texte alternatif</th>
                <td>{{ contenu.alt }}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ contenu.identifier }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ contenu.title }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ contenu.body|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-secondary" href="{{ path('app_contenu_crud_index') }}">Revenir à la liste</a>
<div class="btn-group" role="group" aria-label="show contenu">
    <a class="btn btn-outline-info my-2" href="{{ path('app_contenu_crud_edit', {'id': contenu.id}) }}">Editer</a>
    {{ include('admin/contenu_crud/_delete_form.html.twig') }}
</div>
{% endblock %}
