<form method="post" action="{{ path('app_musician_crud_delete', {'id': musician.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ musician.id) }}">
    <button class="btn">Delete</button>
</form>
