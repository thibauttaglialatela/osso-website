<form method="post" action="{{ path('app_compositor_crud_delete', {'id': compositor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compositor.id) }}">
    <button class="btn btn-outline-danger">Effacer</button>
</form>
