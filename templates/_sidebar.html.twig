<button class="btn btn-info d-lg-none" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasResponsive"
        aria-controls="offcanvasResponsive">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-list" viewBox="0 0 16 16">
        <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
    </svg>
</button>


<div class="sidebar offcanvas-lg offcanvas-start d-lg-flex flex-column flex-shrink-0 p-3 text-white"
     tabindex="-1" id="offcanvasResponsive" data-bs-scroll="true" data-bs-backdrop="false" aria-labelledby="offcanvasResponsiveLabel">
    <div class="offcanvas-header">
        <h5 class="offcanvas-title text-center" id="offcanvasResponsiveLabel">OSSO</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" data-bs-target="#offcanvasResponsive"
                aria-label="Close"></button>
    </div>
    <div class="offcanvas-body d-flex justify-content-center">
        <ul class="nav nav-pills flex-column mb-auto align-items-center justify-content-around h-100">
            <li class="mb-1">
                <a href="{{ path('app_index') }}"
                   class="btn btn-primary btn-lg d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == path("app_index") ? 'active' }}"
                   role="button"
                        {{ app.request.pathinfo == path("app_index") ? 'aria-current = "page"' }}
                >
                    Accueil
                </a>
            </li>
            <li class="mb-1">
                <a
                        class="btn btn-primary btn-lg btn-toggle d-inline-flex align-items-center rounded border-0 collapsed"
                        data-bs-toggle="collapse"
                        data-bs-target="#orchestra-collapse"
                        id="navbarDropdown"
                        aria-expanded="false"
                        role="button"
                >
                    Orchestre
                </a>
                <div class="collapse" id="orchestra-collapse" style>
                    <ul class="btn-toggle-nav list-group">
                        <li class="list-group-item btn btn-primary rounded border-0 {{ app.request.pathinfo == "#" ? 'active' }}" {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                            href="#">Présentation
                        </li>
                        <li class="list-group-item btn btn-primary rounded border-0 {{ app.request.pathinfo == "#" ? 'active' }}" {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                            href="#">Musiciens
                        </li>
                        <li class="list-group-item btn btn-primary rounded border-0 {{ app.request.pathinfo == "#" ? 'active' }}" {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                            href="#">Le répertoire
                        </li>
                    </ul>
                </div>

            </li>
            <li class="mb-1">
                <a href="{{ path('app_index') }}"
                   class="btn btn-primary btn-lg d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == '#' ? 'active' }}"
                   role="button"
                        {{ app.request.pathinfo == '#' ? 'aria-current = "page"' }}
                >
                    Calendrier
                </a>
            </li>
            <li class="mb-1">
                <a href="{{ path('app_index') }}"
                   class="btn btn-primary btn-lg d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == '#' ? 'active' }}"
                   role="button"
                        {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                >
                    Médias
                </a>
            </li>
            <li class="mb-1">
                <a href="{{ path('app_index') }}"
                   class="btn btn-primary btn-lg d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == '#' ? 'active' }}"
                   role="button"
                        {{ app.request.pathinfo == '#' ? 'aria-current = "page"' }}
                >
                    Contactez nous
                </a>
            </li>
        </ul>

    </div>
</div>

