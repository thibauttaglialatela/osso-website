<div class="d-flex flex-column flex-shrink-0 p-3 text-white bg-sidebar sidebar">

    <ul class="nav nav-pills flex-column mb-auto align-items-center justify-content-around h-100">
        <li class="mb-1">
            <a href="{{ path('app_index') }}"
               class="btn btn-primary  d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == path("app_index") ? 'active' }}"
               role="button"
                    {{ app.request.pathinfo == path("app_index") ? 'aria-current = "page"' }}
            >
                Accueil
            </a>
        </li>
        <li class="mb-1">
            <a href="#"
               class="btn btn-primary btn btn-toggle d-inline-flex align-items-center rounded border-0 collapsed"
               data-bs-toggle="collapse"
               data-bs-target="#home-collapse"
               id="navbarDropdown"
               aria-expanded="false"
               role="button"
            >
                Orchestre
            </a>
            <div class="collapse" id="home-collapse" style>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <li class="dropdown-item {{ app.request.pathinfo == "#" ? 'active' }}" {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                        href="#">Présentation
                    </li>
                    <li class="dropdown-item {{ app.request.pathinfo == "#" ? 'active' }}" {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                        href="#">Musiciens
                    </li>
                    <li class="dropdown-item {{ app.request.pathinfo == "#" ? 'active' }}" {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
                        href="#">Le répertoire
                    </li>
                </ul>
            </div>

        </li>
        <li class="mb-1">
            <a href="{{ path('app_index') }}"
               class="btn btn-primary  d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == '#' ? 'active' }}"
               role="button"
                    {{ app.request.pathinfo == '#' ? 'aria-current = "page"' }}
            >
                Calendrier
            </a>
        </li>
        <li class="mb-1">
            <a href="{{ path('app_index') }}"
               class="btn btn-primary  d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == '#' ? 'active' }}"
               role="button"
                    {{ app.request.pathinfo == "#" ? 'aria-current = "page"' }}
            >
                Médias
            </a>
        </li>
        <li class="mb-1">
            <a href="{{ path('app_index') }}"
               class="btn btn-primary  d-inline-flex align-items-center rounded border-0  {{ app.request.pathinfo == '#' ? 'active' }}"
               role="button"
                    {{ app.request.pathinfo == '#' ? 'aria-current = "page"' }}
            >
                Contactez nous
            </a>
        </li>
    </ul>
</div>
